import React from "react";

const Header = ({ navigation }) => {
  return (
    <nav className="navigation">
      <div className="navigation_wrapper"></div>
    </nav>
  );
};

export default Header;
